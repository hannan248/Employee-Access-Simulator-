import 'package:flutter/material.dart';

class AccessSimulatorScreen extends StatelessWidget {
  const AccessSimulatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
